# 商品档案字段检视

更新时间：2026-04-29

## 来源文件

- `/Users/xingyicheng/Downloads/SHEIN上货资料示例.xlsx`
- `/Users/xingyicheng/Downloads/Temu上新资料示例.xlsx`
- `/Users/xingyicheng/Downloads/SHEIN-批量发布商品明细概览.md`

## SHEIN 上货资料字段

`SHEIN上货资料示例.xlsx` 的 `Sheet2` 第 2 行是主表表头，字段如下：

| 字段 | 主要来源 | 当前状态 |
| --- | --- | --- |
| `spu` | SHEIN 平台/人工补充 | 已预留 `product_spu.shein_spu_code`，页面已展示 |
| `SHEIN分类` | SHEIN 类目映射/人工补充 | 已预留 `product_spu.shein_category_name`，页面已展示 |
| `老款号` | MDM/人工补充 | 已预留 `product_spu.old_style_code`，页面已展示 |
| `深绘已有信息` | 人工判断/深绘同步状态 | 已预留 `product_spu.deepdraw_info_status`，页面已展示 |
| `产品线描述` | MDM | 已落库并展示 |
| `SKC描述` | MDM | 已落库，详情款色表展示 |
| `性别描述` | MDM | 已落库并展示 |
| `季节描述` | MDM | 已落库并展示 |
| `中类描述` | MDM | 已落库并展示 |
| `款号` | MDM | 已落库并展示 |
| `SKC编码` | MDM | 已落库，详情款色/SKU 表展示 |
| `企业码` | MDM | 已落库，详情 SKU 表展示 |
| `SKC图片` | MDM/深绘图片 | MDM `pic_url` 已落库，页面当前优先展示深绘图片资产 |
| `SHEIN尺码-录入` | 尺码转换表 | 已预留 `product_sku.shein_size_name`，详情 SKU 表展示 |
| `供货价(人民币)` | 挂牌价 * 供货折扣 | 已预留 `product_sku.supply_price_cny`，详情 SKU 表展示；计算管道待补 |
| `建议零售价(美元)` | `ROUND(挂牌价/7.3, 2)` | 已预留 `product_sku.suggested_retail_price_usd`，详情 SKU 表展示；计算管道待补 |
| `产品毛重/g` | 观远 BI 库存表 | 已预留 `product_sku.gross_weight_g`，详情 SKU 表展示；数据源待接 |
| `颜色编码` / `颜色描述` | MDM | 已落库，详情款色/SKU 表展示 |
| `小类描述` | MDM | 已落库并展示 |
| `面种描述` | MDM | 已落库并展示 |
| `尺码编码` / `尺码描述` | MDM | 已落库，详情 SKU 表展示 |
| `年份` | MDM | 已落库并展示 |
| `挂牌单价` | MDM | 已落库并展示 |
| `供货折扣` | 人工规则，常规 0.4/低倍率 0.45 | 已预留 `product_sku.supply_discount`，详情 SKU 表展示；规则管道待补 |
| `含包装尺寸` | 人工规则/包装规则 | 已预留 `product_sku.package_size_text`，详情 SKU 表展示；规则管道待补 |
| `中文标题` / `英文标题` | 深绘/标题清单 | 已预留并展示，深绘标题可作为兜底 |

`Sheet4` 是标题清单，三列分别是 `款号 / 中文标题 / 英文标题`，适合后续作为标题补充导入来源。

## SHEIN 发布后台字段

`SHEIN-批量发布商品明细概览.md` 提到的后台发布字段及缺口：

| 发布字段 | 需要的商品档案信息 | 当前状态 |
| --- | --- | --- |
| 商品标题 | 英文标题 | 已展示 |
| 商品品牌 | 款号/品牌主数据映射 | 已展示品牌和品牌编码 |
| 商品属性 | 成分来自深绘，织造方式来自 MDM，其余人工判断 | MDM 成分/面种已展示，深绘关键字段已展示；属性模板映射待做 |
| 主规格 | 颜色中文描述 | 已展示 |
| 其他规格 | SHEIN 尺码转换 | 字段已展示，转换表管道待补 |
| 主规格图 | 森马云盘/图片资产 | 深绘商品图已展示；SHEIN 可用图转换待做 |
| 供应信息 | 供货价、SKC 编码、企业码 | 页面已展示，供货价计算待补 |
| 包装信息 | 毛重、包装尺寸、包装类型 | 毛重/尺寸字段已展示，包装类型规则待补 |
| 尺码表 | 深绘尺码表 | 详情页已展示深绘尺码表 |

## Temu 上新资料字段

`Temu上新资料示例.xlsx` 的 `Sheet1` 第 1 行是 SKU 粒度上新表，关键字段包括：

| 字段组 | 字段 | 当前商品档案覆盖情况 |
| --- | --- | --- |
| 平台编码 | `TEMU-SPU`、`TEMU-SKC`、`TEMU-SKC货号` | 尚无 Temu 平台编码表 |
| 渠道/店铺 | `上新日期`、`儿童A链接`、`成人B链接`、`上新店铺/辅助列` | 尚无 Temu 上新任务/店铺维度 |
| 图片 | `产品素材图` | 商品图已展示，但未区分 Temu 素材图 |
| SKU 基础 | `SKC`、`SKU编码`、`企业码`、`颜色描述`、`尺码描述` | 已覆盖 |
| 尺码/岁段 | `规格范围`、`TEMU岁段判断` | `spec_range` 已有，岁段规则待补 |
| 价格 | `挂牌单价`、`考核成本`、`活动价警戒线`、`TEMU上新申报价`、`TEMU最低核价` | 挂牌价已覆盖，其余 Temu 价格规则待补 |
| 库存 | `可用库存`、`运营仓库存`、`逻辑仓库存` | 尚无库存数据源 |
| 商品属性 | `成分信息`、`成分信息-英文`、`品牌`、`产品线`、`性别`、`品类-中类`、`小类`、`年份`、`季节`、`产品标题-公式` | MDM/深绘基础已覆盖，英文成分和 Temu 标题规则待补 |

`Sheet2` 是 Temu 上新判断和库存/资质辅助表，包含 `是否齐断码`、`儿童码段上新`、`备注-上新判断`、`资质安排`、`渠道性质` 等字段。当前商品档案尚未建 Temu 发布准备表，需要后续单独建模。

## 页面调整建议

已完成：

- 列表页保留品牌筛选，并增加 `发品` 列，展示 SHEIN 类目、老款号、深绘资料状态。
- 列表页批量同步改为按钮触发弹窗，弹窗内支持 MDM/深绘、深绘租户选择、批量款号、请求间隔和队列进度。
- 详情页增加 `发品资料` 卡片，集中展示 SHEIN/深绘发品字段。
- 详情页 SKU 表增加供货价、美元建议价、供货折扣、毛重、包装尺寸。
- 详情页增加 `尺码表` 页签，展示深绘返回的尺码表。

后续需要补的数据能力：

- SHEIN 类目映射和属性模板映射，生成平台可用的类目、品牌、属性值。
- SHEIN 尺码转换表导入，将 MDM 尺码转换成 `shein_size_name`。
- 供货价、美元建议价、供货折扣、包装尺寸的规则计算管道。
- 观远 BI 或库存数据接入，补毛重、库存、Temu 库存判断。
- Temu 平台编码、店铺/渠道、资质、核价、上新任务状态的独立建模。
