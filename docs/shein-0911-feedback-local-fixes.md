# SHEIN 9.11 反馈：本地修复与验证

本次仅修改本地代码、读取钉钉和平台元数据、操作本地回归草稿。没有发送群消息、提交 Git、推送、部署或向生产提交商品。

## 原始证据

- 2026-09-11 17:23 黄宇森反馈及最新群记录已通过 dws 读取。既有自动排查任务状态为 completed，原任务没有修改代码。
- 文档 `listingfy测试问题汇总` 当前工作表为 **910、817、812、806、724**，没有名为 9.11 的 sheet。910 页含本次三项问题及截图；已下载整个工作簿并查看截图，对照 817 页。
- `类目枚举值.xlsx`：187 条商品记录，其中 186 条与另一个附件和本地 SHEIN 叶子元数据核对成功，覆盖 48 个小类、55 个叶子类目。24 个小类对应不止一个平台类目，不能直接覆盖成单一映射。
- `SHEIN我的导出类目_中英对照.xlsx` 已读取，按英文名联结，核对中文、叶子 category_id、product_type_id、性别和年龄段。
- 唯一未解析记录：208326120201 / 针织连体衣 / Newborn Baby Bodysuits，业务表中文为 #N/A，不纳入自动匹配。
- 原始文件、sheet JSON、截图、API 回归记录位于 `.codex-tmp/shein-0911/`。该目录已有其他工作，本次仅新增子目录。

## 问题、根因、修改

| 反馈 | 实际证据与根因 | 本地修改 |
| --- | --- | --- |
| 类目匹配失败 | 910 页 208426141211；817 页马甲、羽绒服；旧 fallback 未覆盖鞋类，未消费业务映射。相同小类还可能对应不同形态 | 导入核验后的业务记录；先按款号、再按中类+小类+明确性别/年龄筛选，只有唯一候选自动使用。冲突和维度不足留待人工选叶子类目；不以颜色猜性别 |
| 同款男女第二份提交失败 | 910 页 208426107201 截图明确为本地 `SHEIN 发布范围已有进行中的草稿（draft-001）`；整款互斥且所有草稿 supplier_code 都使用原款号 | 新发布单使用原款号-L草稿ID，首份及 legacy default 保留原编码。继续锁定原款并检查 SKC 重叠、原任务未知/审核状态；互不重叠的独立草稿允许继续。已提交源草稿可派生独立新草稿，不修改源草稿。字段保存/AI补齐/类目记录按草稿隔离；派生时复制字段快照 |
| 关税触发属性无处填写 | 910 页及 817 页；旧逻辑只覆盖少数硬编码条件，未调用正式关联规则接口；发布前还会重新推断并改写已选关税 | 按当前商品属性调用 get-associated-attribute-rules，动态补入必填字段；允许值按 attribute_value_list 与 attribute_value_pre_fill_list 并集处理。字段来自本地平台模板，缺模板/枚举明确报错。保存读回保留可选模板中的已填字段。正式发布及 dry-run 均再次查询规则并校验；提交时保留已选择关税 |

平台约束依据仓库正式文档：`docs/reference/interface-docs/shein_开放平台文档结构化导出.json` 的接口 3001366，以及系统文档 supplier_code 全店唯一要求。接口查询只传商品属性（type 3/4），排除销售/尺码属性。

## 已完成验证

- 浏览器实际打开本地 Vite + 实际 Hono 路由 + PostgreSQL：选择防寒夹克(填充物) → 自动出现填充物 → 选择枚举 → 保存 → 刷新读回；切换普通防寒夹克后撤销动态必填标记，保留已填值，再切回填充种类重新触发。
- 本地草稿 55/56（复用本地样本原款 208126108011，不是业务失败款）：分别设男童大外套、女童大外套，选择不同 SKC。保存读回互不覆盖；平台编码分别为 208126108011-L55、208126108011-L56。
- 发布互斥回归：不重叠 SKC 可继续；重叠 SKC 在审核/已提交/未知任务时阻断；已有同草稿任务仍去重。只测试本地断言和 payload，没有发品。
- dry-run：填充物缺失时增加关联必填错误，补齐后该错误消失。最终 payload 保留关税 ID 1000407/value 1006241 和填充物 ID 1000067/value 1001835。
- 构建与 lint 通过（lint 保留其他页面已有 6 个 hooks 警告）；相关定向回归 96 项通过。最终全量测试 995 项：993 通过、1 跳过、1 失败。失败为既有导出进度 UI 断言，涉及未改动的 async-task-center.tsx 与 shein_platform_products_persistence.test.mjs；两文件与 HEAD 一致。

## 验收限制与待确认

真实平台关联规则接口的只读请求返回 HTTP 401，因此本地页面联动用正式文档结构的受控响应验证，不能宣称已通过真实平台鉴权/规则验收。回归服务器拦截所有其他外网请求。发布前需恢复有效配置，重新查询该接口并核对真实样本，无需生产发品。

本地数据库未包含 208426141211 和 208426107201，故本次依据原始截图和附件定位问题，页面验收使用明确标记的本地样本。业务仍需人工确认每个 SKC 的男女归属；同一 SKC 同时挂男女链接不在本次放行范围。未解析的 Newborn Baby Bodysuits 记录，以及同维度多形态小类，需要明确叶子类目口径。修改尚未发布。本地验证额外派生了草稿 57，确认已提交源草稿可以独立派生且保留字段；55/56/57 已归档，受控回归服务已停止。
