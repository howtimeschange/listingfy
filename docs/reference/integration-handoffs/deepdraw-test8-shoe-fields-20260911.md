# test8 鞋品字段推送验收（2026-09-11）

> 历史探测记录，保留当时结果。最终规则已按接口方确认调整：唯品首列使用标准尺码，四项重量/包装字段从创建起省略；独立行键探测开关及备注首列校验已撤回。最终验收见 [test10](deepdraw-test10-shoe-fields-20260911.md)。

目标：线上 Listingify 草稿1001，款号204426140121-test8，租户电商巴拉巴拉，深绘产品6518125，类目546。只针对该款验证，不表示常驻服务已部署。

## 结论

尚未完成全部业务要求的程序推送。最终完整程序更新返回10200，但严格回读为 `readback_mismatch`，唯品首列别名不符合目标。不能把网页补写、接口受理或单测通过当成推送成功。

- 销售尺码15个、SKU30个、主表/唯品/天猫/抖音各15行，最终资源回读一致；唯品欧洲码/脚长/内长保持26/160/170.32等原标准值。
- 尺码备注已由程序写成 `脚长(15.8-16.2/内长17)`，页面核对15项。
- 鞋垫材质“其他”和件重尺“按规格设置”由程序写入，页面已核对选中。
- 原产国44338：业务值“中国”须在SDK适配层编码为“142”。已通过完整程序更新及资源回读确认；页面选项142对应中国。
- 唯品首列：最终资源无平台后缀的别名仍为 `11623,26码` 等，不符合 `26码(脚长15.8-16.2/内长17)`。
- 唯品重量/包装长宽高：完整更新省略、显式空字符串均未得到页面空值，重新加载后为0。资源中缺少该字段不能证明页面为空。增量空串请求也未报告这些字段更新成功。

## 已排除的协议写法

| 写法 | 实际结果 |
| --- | --- |
| 唯品表行键直接用完整描述 | 返回10200但唯品表消失；已恢复，不能使用 |
| 唯品表增加“尺码”列 | 描述落入法国码数值列，没有写入别名；已用完整基线恢复 |
| 多平台尺码增加VIP列 | 产生 `11623,描述,VIP`，不是网页使用的无平台后缀别名 |
| 多平台尺码增加空表头 | 产生带末尾逗号的条目，同时保留旧别名；不能作为有效方案，已恢复 |
| 全量请求四个字段值为空字符串 | 资源不含字段，页面补0 |
| 增量请求四个字段值为空字符串 | 返回10200，updates不包含这四个字段；不是清空成功 |

尚需确认深绘正式API对于唯品无平台后缀尺码别名和显式清空默认值的可写协议。当前文档和SDK未给出已验证可用的对应格式；不据此断言所有API均不支持。

## 网页操作与证据边界

曾在网页修正原产国、15个唯品别名和四个空值并点击“存草稿箱”。浏览器随后崩溃，但重新打开确认保存生效。用户明确纠正只允许寻找程序推送办法后，停止网页写入，随后仅以程序执行协议探针和最终完整更新。网页补写结果不计入程序成功证据。

增量探针包装器将空标题/价格一并发送，响应列出标题/零售价变化；下一次及最终完整更新已恢复标题和359.9价格，并资源回读确认。未将该探针包装器合入业务代码。

## 代码与证据

规则变更、本次新增原产国编码及唯品首列严格比较仍为工作区改动。已在服务器隔离目录 `/opt/listingfy/tmp/test8-20260911/runtime` 执行指定款重建/更新，未替换常驻生产代码。最终草稿状态为readback_mismatch。

原始请求与响应在服务器 `/opt/listingfy/tmp/test8-20260911/`，本地副本及运行记录在 `/tmp/listingify-test8-20260911/`：

- `publish-output2.log`：初次接口结构校验通过但页面不满足要求。
- `resource-ui.json`：网页补写后的资源，仅供协议对照。
- `country-empty-readback.json`、`alias-column-readback.json`、`alias-vip-readback.json`、`alias-default-readback.json`：程序探针回读。
- `final-program-publish.log`：最终程序推送及严格回读，恢复探针改动，首列仍不通过。
- `final-tests-tsx.log`：285项测试通过。直接node运行TS依赖曾因模块解析失败，改用仓库要求的tsx后通过。
