# 羽绒服字段反馈修复与测试款验收（2026-09-11）

原款 `202426107020`，原草稿1102，原深绘商品6538499。测试款 `202426107020-test`，草稿1138，深绘商品6538629。

## 修复

- 销售尺码备注采用 `(充绒量72g)`；同时修复 SDK 适配器移除括号的问题，覆盖创建和完整更新。
- 详情页面料保留源文案所有颜色、面料、里布、罗纹除外、填充物、绒子含量和其余部位信息。
- 百分比成分只解析首个主面料段，遇下一部位、颜色或件数分段停止，避免把不同部位/颜色累加。无效或超100%的人工/AI百分比在草稿校验和发布报文校验中阻断。
- 京东发货地固定 `浙江,杭州`，覆盖旧固定映射 `浙江杭州`。
- 同时有白鸭绒、灰鸭绒时优先匹配鸭绒；没有鸭绒选项且支持多选时保留两种。支持成分分布在不同文案行的情况。

## 实际验收

使用生产隔离运行目录加载本地修改，重新构建测试款来源字段。原款的充绒量、尺码表、安全等级、执行标准等既有来源证据用于补齐测试款。没有修改原款，没有提交深绘网页表单。

创建、完整补写、SDK括号修复后的补写均返回业务码10200。独立资源回读最终状态 `readback_verified`，10项独立API断言通过。款号查询曾返回频率限制；使用创建后资源返回的真实UID `aa0e9ec7c0df4de8b4fe5b9371238045` 完成最终独立回读。数字商品ID不能当作此接口的UID。

| 反馈项 | API / SDK证据 | 新打开的深绘页面 |
| --- | --- | --- |
| 尺码备注 | 最终SDK报文8项括号完整；资源接口不返回此备注，比较器明确要求UI核验 | 110/120/130/140/150/160/165/170cm 分别显示 `(充绒量72g)` / 81g / 92g / 103g / 117g / 131g / 143g / 151g，均带括号 |
| 详情页面料 | 与原款完整来源文案一致，仅冒号全角归一 | 灰红色调与咖色调各8行完整，含白鸭绒/灰鸭绒及85%绒子含量 |
| 三组成分比例 | 聚酯纤维100%；京东为涤纶(聚酯纤维)100%；抖音为聚酯纤维100% | 各一行100%，没有重复行或超限 |
| 京东发货地 | 地区ID1213 | 浙江 / 杭州市 |
| 填充物 | 鸭绒 | 鸭绒 |

16 SKU、8个销售尺码及相关尺码表回读比较通过。

## 验证与交付边界

271项针对性测试通过；web lint 0错误、6条已有警告；web build通过；diff check通过。两份修改源文件本地/隔离目录SHA256一致。

本次代码交付为本地 Git 提交，未推送，主服务尚未部署。测试款已经实际写入深绘。修复为通用规则，没有按该测试款号硬编码；线上主服务部署后，新建或重新构建的商品才会应用，已有商品不会自动改写。

证据目录 `/tmp/listingify-down-20260911/evidence/`：创建/更新/修复SDK报文及回执、final-resource.json、final-draft.json、readback-result.json、verification.json、ui-verification.json。服务器隔离目录 `/opt/listingfy/tmp/down-20260911/`。

测试款页面：https://www.deepdraw.biz/authorized/merchant/product/6538629/edit/new
