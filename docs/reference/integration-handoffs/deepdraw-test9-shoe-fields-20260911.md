# test9 鞋品创建与回填验收（2026-09-11）

> 历史探测记录，保留当时结果。最终规则已按接口方确认调整：唯品首列使用标准尺码，四项重量/包装字段从创建起省略；独立行键探测开关及备注首列校验已撤回。最终验收见 [test10](deepdraw-test10-shoe-fields-20260911.md)。

- 款号：204426140121-test9；生产草稿：1066；深绘商品：6537325。
- 使用隔离运行目录执行生产 API 创建及更新，未部署主服务代码；本轮没有网页提交。
- 创建后完整更新成功，最终保留 30 SKU、主/唯品/天猫/抖音 4 张尺码表各 15 行。

## 实测矩阵

| 测试 | 发送与回读结果 |
| --- | --- |
| 创建时独立唯品行键 + 四项显式空字符串 | 10497，参数错误明确包含唯品表及四项重量/包装字段；未创建成功 |
| 正常行键创建并补写 | 成功创建商品 6537325；业务验收状态 readback_mismatch |
| 唯品独立行键，四项空值不参与 | 实际 SDK 请求保留 26码(脚长15.8-16.2/内长17)；10200，但回读唯品表消失，其余三表存在 |
| 四项先写 1 | 10200；API 四项 texts=[1]；新页面四项均为 1；同时恢复正常唯品表 |
| 四项再显式写空字符串 | 10200；API 四项字段消失；新页面均显示 0；四张表完整 |

## 最终页面只读核验

- 15 个选中尺码备注均为脚长(区间/内长数值)。
- 原产国控件值 142（中国）；鞋垫材质其他；件重尺按规格设置勾选。
- 唯品首列仍为 26码，未保存完整描述。数据列为欧洲码、脚长、鞋内长，没有额外单独尺码数据列。
- 四项测试值 1 已清除，页面呈现 0，不能声称页面空白已修复。

## 结论与边界

旧适配器确实归一化唯品行键。但给唯品单独开例外后，深绘更新仍不能保存独立行键，且静默丢失整张唯品表。例外开关仅用于探测，不能作为成功修复默认启用。

四项空字符串在更新时可清掉已写入的值，API 资源不再包含它们；页面却回显 0。创建与更新的空值行为也不同。尚未找到同时满足唯品完整首列和页面四项留空的推送方法。

目标测试 286/286 通过，git diff --check 通过；测试通过不等于远端需求验收通过。

证据目录：/tmp/listingify-test9-20260911/evidence/。关键文件：stable-create-response.json、stable-create-result.json、vip-independent-request.json、vip-independent-response.json、vip-independent-readback.json、blank-seed-readback.json、blank-clear-request.json、blank-clear-readback.json。服务器副本：/opt/listingfy/tmp/test9-20260911/。
