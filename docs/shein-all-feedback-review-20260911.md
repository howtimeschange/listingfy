# SHEIN 全反馈复核（2026-09-11）

已核对 910、817、812、806、724 全部文字与截图，包括 812 第9/10行的纯截图。以下状态只代表本地证据，不代表线上已发布。

本轮修复：当前编辑标题传给单字段 AI；批量 AI 先保存；生成不覆盖未保存编辑；翻译保留有效信息；描述始终展示且不截断，人工描述优先于旧草稿快照；禁止整张主图自动冒充色块图。

业务确认已发送至原反馈群并确认发送成功，消息 msg062vQfpuyjgldSJeoAhbPQ==。等待标题来源和 208326120201 类目答复。

## 910

| 原行 | 修复状态 | 修复内容/卡点 |
|---|---|---|
| 2 | 本地已修复，待发布验证 | 接入业务附件经核对的 186 款类目映射，依次使用款号、中小类和明确性别年龄，仅唯一且本地元数据有效时自动应用；歧义保留人工选择。未部署，需真实反馈款线上回归。 |
| 3 | 待业务确认，尚未解决 | 208426141211 当前读取深绘商品档案主标题；截图可能是渠道标题，未擅自切换来源。已在原反馈群 @黄宇森 确认应取字段/渠道，等待回复。 |
| 4 | 本地已修复，待发布验证 | 拆分草稿使用独立平台 supplier_code，保留原款号；按 SKC 防重复，允许男女不同 SKC 分别发布；已发布草稿可复制，字段按草稿隔离。本地隔离/重复阻断/载荷回归通过；男女归属仍需人工确认。 |
| 5 | 本地已修复，平台验证受阻 | 按当前属性调用 SHEIN 关联规则，动态显示必填字段及枚举；保存、预检、发布统一验证。本地规则夹具及持久化回归通过；真实平台接口当前 HTTP 401，尚未验证真实商品发布。 |

## 817

| 原行 | 修复状态 | 修复内容/卡点 |
|---|---|---|
| 2 | 本地已修复，待发布验证 | 接入业务附件经核对的 186 款类目映射，依次使用款号、中小类和明确性别年龄，仅唯一且本地元数据有效时自动应用；歧义保留人工选择。未部署，需真实反馈款线上回归。 |
| 3 | 部分解决，待补源数据 | 已有后台毛重同步：按 SKU/条码匹配，仅补空值，保留人工录入；尺码按类目枚举和转换规则匹配，未匹配允许人工选择并阻断发布。不编造重量/尺码；反馈款源数据仍需业务补齐并用真实商品验证。 |
| 4 | 本地已修复，平台验证受阻 | 按当前属性调用 SHEIN 关联规则，动态显示必填字段及枚举；保存、预检、发布统一验证。本地规则夹具及持久化回归通过；真实平台接口当前 HTTP 401，尚未验证真实商品发布。 |
| 5 | 本地已修复，待发布验证 | 接入业务附件经核对的 186 款类目映射，依次使用款号、中小类和明确性别年龄，仅唯一且本地元数据有效时自动应用；歧义保留人工选择。未部署，需真实反馈款线上回归。 |
| 6 | 部分解决，待补源数据 | 已有后台毛重同步：按 SKU/条码匹配，仅补空值，保留人工录入；尺码按类目枚举和转换规则匹配，未匹配允许人工选择并阻断发布。不编造重量/尺码；反馈款源数据仍需业务补齐并用真实商品验证。 |
| 7 | 本地已修复，平台验证受阻 | 按当前属性调用 SHEIN 关联规则，动态显示必填字段及枚举；保存、预检、发布统一验证。本地规则夹具及持久化回归通过；真实平台接口当前 HTTP 401，尚未验证真实商品发布。 |
| 8 | 本地已修复，待发布验证 | AI 单字段携带当前未保存中文标题；批量 AI 先保存编辑；生成返回不刷新覆盖其他编辑。翻译要求保留功能/材质/季节等有效信息，不补加原文未写的品牌；缺少配置明确报错。草稿字段隔离保存，待真实模型及线上回归。 |

## 812

| 原行 | 修复状态 | 修复内容/卡点 |
|---|---|---|
| 2 | 本地已修复，待发布验证 | 移除发布准备阶段把整张主图当色块图的自动复制，并排除历史 SOURCE_FALLBACK/SKC_SOURCE_IMAGE 色块占位图。多 SKC 缺真实色块图会明确阻断，单 SKC 按规则可选；需上传真实色块图，尚未线上发布验证。 |
| 3 | 部分解决，待真实 AI 验证 | AI 调用已有场景路由与错误提示；此次补齐当前编辑标题上下文，失败不覆盖输入，不用通用短标题冒充翻译。当前本地页面验证了编辑保留，真实模型调用仍需有效配置/服务验证。 |
| 4 | 本地已修复，待发布验证 | AI 单字段携带当前未保存中文标题；批量 AI 先保存编辑；生成返回不刷新覆盖其他编辑。翻译要求保留功能/材质/季节等有效信息，不补加原文未写的品牌；缺少配置明确报错。草稿字段隔离保存，待真实模型及线上回归。 |
| 9 | 本地已修复，待发布验证 | AI 单字段携带当前未保存中文标题；批量 AI 先保存编辑；生成返回不刷新覆盖其他编辑。翻译要求保留功能/材质/季节等有效信息，不补加原文未写的品牌；缺少配置明确报错。草稿字段隔离保存，待真实模型及线上回归。 |
| 10 | 本地已修复，待发布验证 | AI 单字段携带当前未保存中文标题；批量 AI 先保存编辑；生成返回不刷新覆盖其他编辑。翻译要求保留功能/材质/季节等有效信息，不补加原文未写的品牌；缺少配置明确报错。草稿字段隔离保存，待真实模型及线上回归。 |

## 806

| 原行 | 修复状态 | 修复内容/卡点 |
|---|---|---|
| 1 | 已有实现，待线上回归 | SKC 发布标题已优先使用当前商品草稿标题；保存后刷新发布快照，避免旧款色短标题覆盖。源码/回归覆盖，尚未真实平台回读。 |
| 2 | 本地已修复，待发布验证 | 移除发布准备阶段把整张主图当色块图的自动复制，并排除历史 SOURCE_FALLBACK/SKC_SOURCE_IMAGE 色块占位图。多 SKC 缺真实色块图会明确阻断，单 SKC 按规则可选；需上传真实色块图，尚未线上发布验证。 |
| 3 | 本地已修复，待发布验证 | 商品描述有值时仍显示在基础资料，可编辑保存并进入发布描述；去掉源描述 160 字截断。已在本地页面确认描述入口，并保存、重新打开及 API 回读确认人工描述不被旧快照覆盖。 |
| 4 | 已有实现，待线上回归 | 发布前将深绘来源图通过 SHEIN 转链/上传处理，并校验最终载荷均为平台可用 URL；主图最多一张，导入图按类型处理。已有转链、图片类型与失败分支测试；真实平台上传受当前凭据限制，待验证。 |
| 5 | 部分解决，待真实 AI 验证 | AI 调用已有场景路由与错误提示；此次补齐当前编辑标题上下文，失败不覆盖输入，不用通用短标题冒充翻译。当前本地页面验证了编辑保留，真实模型调用仍需有效配置/服务验证。 |
| 6 | 本地已修复，待发布验证 | 无类目时仍提供修改类目入口；业务映射与本地有效叶子类目检查已接入。208326140010 仍需实际商品选择正确性别/类目并线上回归。 |
| 7 | 待业务确认，尚未解决 | 旧截图为面料/织造方式冲突；最新附件又将 208326120201 记为针织连体衣且类目 #N/A。不能猜测商品材质或改关税规避校验；已 @黄宇森 确认实际品类，待答复后继续。 |

## 724

| 原行 | 修复状态 | 修复内容/卡点 |
|---|---|---|
| 2 | 部分解决，待平台/业务确认 | 方形图已有 3:4 自动居中裁切及正方形复用测试；关联规则动态必填已修复，里衬可填写。208326120201 源品类冲突待作者确认；真实规则接口 HTTP 401，待平台验证。 |
| 3 | 已有实现，待线上回归 | 截图为成分百分比及主图数量错误：已有匹配后的成分比例归一到 100%，每个 SKC 恰好一张主图校验；缺少真实成分时不猜测。相关回归通过，待该款真实发布验证。 |
| 4 | 本地已修复，平台验证受阻 | 按当前属性调用 SHEIN 关联规则，动态显示必填字段及枚举；保存、预检、发布统一验证。本地规则夹具及持久化回归通过；真实平台接口当前 HTTP 401，尚未验证真实商品发布。 |
| 5 | 已有实现，待线上回归 | 与本 sheet 第3行重复：成分比例及主图数量已有校验/归一逻辑；未进行该款线上发布，不能标为线上已解决。 |
| 6 | 本地已修复，平台验证受阻 | 按当前属性调用 SHEIN 关联规则，动态显示必填字段及枚举；保存、预检、发布统一验证。本地规则夹具及持久化回归通过；真实平台接口当前 HTTP 401，尚未验证真实商品发布。 |
| 7 | 部分解决，待补真实属性 | 基础必填属性按 SHEIN 类目元数据显示；关联条件属性动态加载并在提交前校验。缺少真实成分、材质、护理等来源仍需人工维护，禁止 AI 猜测；当前未完成该款真实平台验证。 |

## 验证边界

全量回归 997 项发现两处本轮源码断言需更新，已随接口变化更新；最终相关定向 101 项通过。剩余一处原有异步任务导出进度断言失败，与改动无关（原始基线已确认）。Web 构建通过，lint 0 errors / 6 既有 warnings。页面验证标题、描述编辑保存及重新打开，API 回读一致；旧伪色块图在界面被排除。真实 SHEIN 规则接口此前返回 HTTP 401，本轮未重复同凭据请求或执行线上发布。
806 插入表头行后逐单元格比较，原有文字及图片对象保持不变。

## 发布前复核补充

用户确认 208426141211 应以同步后的深绘档案中文标题为准，之前为档案未更新。生产只读核对已确认内容包标题为“巴拉巴拉童鞋儿童雪地靴男童女童2026冬季新款婴儿宝宝棉鞋厚靴子”，无需改取渠道字段。

已把过时的导出进度源码断言改为运行时行为检查：生成中最多 99%，完成 100%。最终全量测试 998 项，997 通过、1 跳过、0 失败。
